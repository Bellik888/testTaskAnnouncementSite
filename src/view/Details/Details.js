export const Details = ({ title, description }) => {
  return (
    <div>
      <p>{title}</p>
      <p>{description}</p>
      <div>
        <button></button>
        <button></button>
      </div>
    </div>
  );
};
